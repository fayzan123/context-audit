# Outside

Sibling of `skill-dir/`, referenced from it as `../outside/secret.md`. It
EXISTS on purpose: a root-escaping reference must be rejected outright, so it
may appear neither in `missing` (it is there) nor in `checked` (it is not the
skill's to ship).
